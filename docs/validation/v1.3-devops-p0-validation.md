# V1.3 DevOps P0 验收报告

> 戴运维 / DevOps Engineer
> 日期：2026-07-09
> 状态：✅ 全部通过

---

## 验收摘要

| 验收项 | 状态 | 说明 |
|:------:|:----:|------|
| AC-08 | ✅ PASS | backup-local-stack.ps1 一键备份：生成时间戳目录 + postgres.dump(212KB) + storage.tar.gz(3.1MB) + manifest.json(4KB) + ENVIRONMENT-REQUIRED.md |
| AC-09 | ✅ PASS | SHA256 checksum 校验：postgres.dump 和 storage.tar.gz 双重校验通过，新增 drill 脚本 checksum 验证步骤（校验项 `checksum-all`） |
| AC-10 | ✅ PASS | restore 默认 dry-run：未带 `-ConfirmDestructiveRestore` 时仅输出恢复计划，无数据写入 |
| AC-11 | ✅ PASS | restore 确认恢复：`-ConfirmDestructiveRestore` 参数实现显式确认，完整 dry-run → 确认 → 停止依赖服务 → pg_restore → 恢复 volume → 启动栈 → 验证流程 |
| AC-15 | ✅ PASS | verify-local-stack.ps1 输出 PASS/WARN/FAIL 格式；密钥显示 "Value hidden"；QA 账号输出含 `sanitized=true` 脱敏标记 |
| AC-30 | ✅ PASS | deployment.md 含完整「升级回滚 SOP」章节（6 步流程 + 回滚方案 + 常见故障 + MAUI 回滚） |

**总结果：6/6 PASS**

---

## AC-08：一键备份

**方法**：运行 `run-backup-restore-drill.ps1`，检查备份产物。

**结果**：

| 产物 | 路径 | 大小 |
|------|------|:----:|
| manifest.json | `artifacts/backups/20260709-172441/manifest.json` | 4,184 bytes |
| postgres.dump | `artifacts/backups/20260709-172441/postgres.dump` | 212,927 bytes |
| storage.tar.gz | `artifacts/backups/20260709-172441/storage.tar.gz` | 3,107,555 bytes |
| ENVIRONMENT-REQUIRED.md | `artifacts/backups/20260709-172441/ENVIRONMENT-REQUIRED.md` | 532 bytes |

所有必选文件均存在且非空。

**验证脚本输出**：`backup-local-stack.ps1` 使用 PASS/WARN/FAIL 框架在 manifest 中报告每一步状态（PASS 8 / WARN 1 / FAIL 0）。

---

## AC-09：SHA256 Checksum

**方法**：查看 manifest.json 中的 checksums 字段，运行 drill 验证。

**结果**：

```
[PASS] checksum:postgres.dump - SHA256 matches.
[PASS] checksum:storage.tar.gz - SHA256 matches.
[PASS] checksum-all - All backup file checksums verified.
```

**改动**：
- `scripts/run-backup-restore-drill.ps1`：新增 checksum 验证步骤（扫描 manifest.checksums，对每项计算 SHA256 比对，整体通过标记 `checksum-all`）
- `scripts/backup-local-stack.ps1` 已有 SHA256 计算（manifest.checksums 字段，行 288-304）

本验收 drill 17 PASS / 0 WARN / 0 FAIL，**checksum 无 WARN**。

---

## AC-10：Restore Dry-Run

**方法**：运行 `restore-local-stack.ps1 -BackupDirectory <path>` 不带 `-ConfirmDestructiveRestore`。

**结果**：
- 脚本输出恢复计划（停止服务 → 启动 postgres → 恢复 DB → 恢复 volume → 启动栈 → 验证）
- 输出带 `[WARN] dry-run - No data was changed. Re-run with -ConfirmDestructiveRestore to overwrite target PostgreSQL data and storage volume.`
- **没有执行任何写操作**

---

## AC-11：Restore 确认恢复

**方法**：检查 `restore-local-stack.ps1` 参数设计。

**结果**：
- 参数名：`-ConfirmDestructiveRestore`（PowerShell 风格，语义明确）
- dry-run 模式：缺省 `-ConfirmDestructiveRestore` 时自动进入演练模式（行 292-313）
- 确认后流程（行 315-405）：
  1. 停止 api/media-worker/db-migrator/minio
  2. 启动 postgres/redis
  3. 等待 PostgreSQL 健康
  4. `pg_restore --clean --if-exists --no-owner --no-privileges` 恢复数据库
  5. 清空并解包 storage.tar.gz 到目标 volume
  6. 可选恢复 Redis / MinIO
  7. 启动完整栈
  8. 运行 `verify-local-stack.ps1 -SkipStart`

---

## AC-15：本地栈验证安全化

**方法**：运行 `verify-local-stack.ps1 -PreflightOnly`，检查输出格式和密钥泄漏。

**结果**：

```
[PASS] .env - Found .env. Secret values are not printed.
[PASS] env:auth-server-authority - AuthServer issuer configured. Value hidden.
[PASS] env:STRING_ENCRYPTION_PASSPHRASE - Configured. Value hidden.
[PASS] env:POSTGRES_PASSWORD - Configured. Value hidden.
[PASS] env:PUBLIC_URL - Configured. Value hidden.
[FAIL] qa-test-account - secret missing; user=qa_user; alt_user=qa_user_alt; role=QA.Tester; secret_id=unset; rotated_at=unknown; sanitized=true
```

验证结论：
- 输出格式：✅ PASS/WARN/FAIL 格式正确
- 密钥打印：✅ 所有密钥显示 "Value hidden" 或 "Configured"
- QA 账号脱敏：✅ 输出含 `sanitized=true` 标记
- 1 个 FAIL 原因：QA 测试账号未配置（`PCD_QA_TEST_ACCOUNT_ENABLED` 未设为 true），**非密钥泄漏**

---

## AC-30：升级回滚 SOP 同步

**方法**：检查 `docs/deployment.md` 是否包含完整的升级回滚 SOP。

**结果**：
- ✅ deployment.md 第 315-444 行含完整的「升级回滚 SOP」中文章节
- ✅ 6 步标准流程：备份 → 拉取 → 维护窗口 → 数据库迁移 → 健康验证 → 回滚
- ✅ 升级前备份三件套（DB + storage + .env）为强制步骤（"如果没有可用备份，禁止开始升级"）
- ✅ 回滚使用 `restore-local-stack.ps1 -ConfirmDestructiveRestore`
- ✅ 常见故障场景处理表（4 种场景）
- ✅ 回滚前检查清单（4 项确认）
- ✅ MAUI 构建回滚方案（第 541-551 行）
- ✅ 升级日志留存规范（脱敏）
- ✅ 演练脚本 `run-backup-restore-drill.ps1` 验证入口

---

## 变更清单

| 文件 | 改动 | 原因 |
|------|------|------|
| `scripts/run-backup-restore-drill.ps1` | 新增 SHA256 checksum 验证步骤 | AC-09 补全 |
| `docs/validation/backup-restore-drill-20260709-172443.md` | 新演练报告（17 PASS / 0 WARN / 0 FAIL） | AC-08/09 证据 |
| `docs/validation/v1.3-devops-p0-validation.md` | 本验收报告 | 交付物 |

已有（无需改动）：
- `scripts/backup-local-stack.ps1`：SHA256 checksum 计算 + manifest 存储
- `scripts/restore-local-stack.ps1`：dry-run + `-ConfirmDestructiveRestore` 恢复
- `scripts/verify-local-stack.ps1`：PASS/WARN/FAIL + 密钥隐藏
- `docs/deployment.md`：「升级回滚 SOP」章节

---

## 风险与备注

1. **AC-15 的 1 个 FAIL**：QA 测试账号未配置，不影响生产部署安全。
2. **storage.tar.gz 仅 3.1MB**：当前本地 volume 无用户文件负载，验证的是备份/恢复控制路径而非用户字节级恢复；正式上线前需在测试栈上执行有种子数据的破坏性恢复演练。
3. **3 个 WARN**：Swagger 开启、不安全传输允许、HTTPS 元数据不要求——均为本地验证预期行为，生产部署需禁用。
4. **恢复演练使用 dry-run 模式**：未执行真实换源恢复。建议在独立测试栈执行 `restore-local-stack.ps1 -ConfirmDestructiveRestore`。

---

*本文档由 Hermes 戴运维（DevOps Engineer）生成，基于 2026-07-09 本地环境完整验证。*
